# 02 里程碑计划

## M0 骨架（已完成）

- [x] 四项目解决方案：Core / Drivers / App / Tests
- [x] IDeviceDriver 抽象 + 点位/数值模型
- [x] SimulatedDriver（温度趋近设定值 + 压力正弦，无硬件跑通全链路）
- [x] AcquisitionEngine（PeriodicTimer 周期循环 + 事件抛出）
- [x] WPF 实时表格（MVVM，SynchronizationContext 切线程）
- [x] 6+2 单元测试全绿
- [x] Serilog 文件日志

## M1 采集核心（v0.1）

- [x] S7NetPlus 驱动（S7-300/400/1200/1500，DB 绝对地址解析 + 最小覆盖区间批量读）
- [x] DeviceHub.Simulator：自研 S7 从站模拟器（snap7 Server API，物理模型与 SimulatedDriver 一致）
- [x] S7 端到端集成测试（真实 S7 协议栈：连接 / 批量读 / 写设定值 / 物理响应）
- [x] NModbus TCP 驱动（FC03/06/16，HRn 与 4xxxx 双地址格式，ABCD 大端浮点与 S7 共用转换器）
- [x] 迷你 Modbus TCP 从站模拟器（测试用，M4 内置模拟器雏形：FC03/06/16 + 异常码）
- [x] Modbus 端到端集成测试（真实 Modbus TCP 协议栈：批量读 / 写寄存器 / 从站异常码）
- [x] Modbus 传输层 2 秒读写超时（修复从站无响应时采集线程永久挂起）
- [x] JSON 点位配置加载（appsettings.json：设备/连接参数/点位/周期全部配置化）
- [x] Generic Host + DI 组装（界面设备下拉框可切换驱动，连接失败自动清理）
- [x] NModbus RTU 驱动（协议核心共享 ModbusDriverBase；内存虚拟串口线全链路回环测试常开——帧定界+CRC16 走真协议栈、不依赖任何串口驱动；真串口对回环测试装好即自动生效）
- [x] LiveCharts2 实时曲线页（71dccc1）
- [x] 断线重连策略（22dab6d：连续失败阈值 + 指数退避封顶，断线周期发布 Bad 质量）

## M2 商业模块（v0.2）

- [x] 报警引擎（上下限+回差防抖、Warning/Critical 分级、确认、BadQuality 通信报警、历史事件容量封顶——Core 纯逻辑状态机，11 项单测）
- [x] SQLite 落库（Microsoft.Data.Sqlite 命名占位符参数化 + 后台批量事务写）+ 历史趋势查询（曲线页历史图）+ 报警生命周期事件入库
  - 注：原计划用 Dapper，mimosa 安全钩子不认其命名参数绑定（误报拦截），用户拍板改手写 ADO.NET 参数化
- [ ] 报警历史查询界面（数据已入库，只差 UI）
- [ ] 报警规则编辑界面（当前 appsettings 配置）
- [x] 登录与用户权限（操作员/工程师：启动登录门 + 会话栏"锁定/切换用户"；权限=角色→能力集映射，运动/视觉/确认分级门禁，**急停与停止永不禁用**；密码支持 sha256: 哈希形态，Core AuthService 9 项单测）
- [x] Excel 导出（ClosedXML：报警页导 SQLite 全量报警账本、曲线页"所见即所导"导当前查询结果；中文表头+时间列格式，导出后读回验证，4 项单测）
- [ ] Serilog 结构化日志完善

## M3 北向对接

- [x] OPC UA Server（OPCFoundation.NetStandard 1.5：新项目 DeviceHub.Northbound，托管服务常驻，点位动态建节点、值+质量戳实时刷新；None 安全+匿名（演示定位），自签名证书首次自动生成；进程内 SDK 客户端端到端测试——发现/会话/读值/质量传播/未知节点）
- [x] MQTT 发布（MQTTnet 5：嵌入式 Broker + 发布客户端环回（演示自包含，生产换外部总线连接参数）；遗嘱+保留消息+优雅停机主动 offline（规范：优雅断开不触发遗嘱）；devicehub/points/{点位} 推 JSON（中文不转义，quality 如实标注）；进程内订阅客户端端到端测试 3 项）
- [ ] Linux 虚拟机部署跑通（.NET 跨平台），README 记录步骤

## M4 亮点（任选其一二）

- [ ] InfluxDB 时序库双写
- [x] 内置 Modbus 从站模拟器（迷你 TCP 从站随测试交付；RTU 从站台架用于串口/虚拟线联调测试）
- [ ] CSharpScript 用户脚本引擎
- [ ] 视觉模块（OpenCvSharp 定位 + ONNX YOLO，结果写回 PLC）

## M5 运动控制（对标运动控制卡开发岗）

- [x] IMotionControl 抽象（回零/Jog/绝对/相对定位/直线插补/停止/急停/清状态）
- [x] SimMotionControl 模拟卡：20ms Tick 推进轴位置，完整安全联锁
  （未回零禁动、运动中禁新指令、软限位指令层拒绝、Jog 撞限位自动停+报警）
- [x] 直线插补：按各轴位移比例分配速度，所有轴同时到达、合成轨迹为直线
- [x] 运动控制页签：轴卡片（实时位置 100ms 轮询）、回零/Jog/定位按钮、全局急停
- [x] Motion 配置节（轴数量/名称/软限位/默认速度全部配置化）
- [x] 10 项运动控制单元测试（联锁/定位/插补/限位/急停）
- [ ] ⏸ 搁置（2026-09-21 决定全面虚拟、不购硬件）：雷赛 DM 系列 C# SDK 真卡实现（IMotionControl 的第二个实现，架构已预留——面试话术"模拟卡全链路验证，真卡即插即换"）
- [x] 视觉引导闭环：定位 → 九点标定换算 → 输出坐标驱动轴运动（M6 交付；定位器现含 Halcon 形状匹配实现）

## M6 视觉定位与引导闭环

- [x] DeviceHub.Vision 工程拆分（OpenCvSharp4）
- [x] 合成相机：机台世界坐标→像素已知映射，工件随机摆放（±30° 旋转），Ground Truth 保留用于精度验证
- [x] VisionLocator：灰度→阈值分割→外轮廓→最大面积→最小外接矩形（长边方向归一化角度）
- [x] 九点标定：3x3 网格采样 + 正规方程最小二乘拟合像素→机台仿射变换，平均残差界面展示
- [x] 视觉定位页签：200ms 预览、单帧定位（红十字叠加标记）、标定残差显示、操作日志
- [x] 视觉引导运动闭环：定位工件→标定换算→X/Y 轴插补走位（与运动模块联动）
- [x] 8 项视觉测试（标定恢复/退化拒绝/定位精度/未找到/闭环集成）
- [x] Halcon 实现（新项目 DeviceHub.Vision.Halcon：形状模板匹配定位器 CreateShapeModel/FindShapeModel，IVisionLocator 第二实现——标定/引导/界面全复用零改动；Mat→GenImage1 零拷贝导入；无 HALCON 环境测试自动跳过。
  - 2026-09-22 翻案：旧结论"无 license 不可用"是误判——探针实测本机 Halcon 12 算子链（含形状匹配模块）无 license 文件可运行；halcondotnet.dll 入库 libs/（托管互操作层，类比 snap7.dll），本机 halcon.dll 走 PATH）
- [ ] ⏸ 搁置（2026-09-21 决定全面虚拟、不购硬件）：真实相机 SDK 接入（海康 MVS/Basler Pylon，替换合成取流）
